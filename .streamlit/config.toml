# Настройки клиентской версии дашборда.

[client]
# Убирает из интерфейса кнопки «Fork», значок GitHub и меню разработчика.
# Клиенту они не нужны, а вести его в репозиторий с кодом незачем.
toolbarMode = "minimal"

[theme]
primaryColor = "#0f766e"
